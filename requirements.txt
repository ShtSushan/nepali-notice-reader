# ─────────────────────────────────────────
#   Nepali Notice Reader — Python Dependencies
#   Install: pip install -r requirements.txt
# ─────────────────────────────────────────

# ── API Framework ──
fastapi
uvicorn
python-multipart        # for file upload handling

# ── Environment ──
python-dotenv

# ── OCR ──
pytesseract             # Python wrapper for Tesseract
Pillow                  # image processing
pdf2image               # converts PDF pages to images for OCR

# ── AI / LLM ──
groq                    # Groq API (translation, summarization, Q&A)
google-genai            # Gemini API (optional fallback OCR)

# ── Embeddings ──
sentence-transformers   # multilingual embeddings (local, free)

# ── Database ──
supabase                # Supabase Python client (PostgreSQL + pgvector)

# ── Notebook (development only) ──
jupyter
notebook
ipykernel